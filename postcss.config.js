// PostCSS minimal untuk Tailwind + Autoprefixer.
// Toleran: kalau plugin belum terpasang, error akan jelas di terminal.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
}
